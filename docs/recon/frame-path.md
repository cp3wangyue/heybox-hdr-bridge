# 真实帧路径图（Gate P2 输出，§6）

> 这是整个项目最关键的证据文档。没有下面五项，不进入写帧阶段。
> 客户端版本：_______　日期：_______　HDR 状态：_______

## 五项必答

| # | 项目 | 结论 | 证据（日志行/调用栈/录屏时间点） |
| --- | --- | --- | --- |
| 1 | 捕获 API（WGC / DXGI Duplication / 共享纹理 / 自研） | | |
| 2 | 捕获纹理格式（Format + 尺寸 + BindFlags/MiscFlags） | | |
| 3 | 颜色/格式转换节点（谁、在哪里、转成什么） | | |
| 4 | 编码器输入格式（NV12 / BGRA / P010 / I420-CPU…） | | |
| 5 | HDR 信息在哪一步首次丢失 | | |

## 对照追踪数据（§6.3）

| 测试 | HDR | 捕获格式 | 编码器输入 | 观察 |
| --- | --- | --- | --- | --- |
| T2-1 | 关闭 | | | 正常路径格式 |
| T2-2 | 开启 | | | 格式是否 FP16/RGB10A2；是否数值裁切 |
| T2-3 | 开启+SDR 桌面 | | | SDR-on-HDR 缩放行为 |
| T2-4 | 开启+HDR 游戏 | | | 高光在哪一节点后裁白 |

## 判定树执行结果（§6.4）

```
capture_format = ?
→ R16G16B16A16_FLOAT : scRGB/FP16 假设
→ R10G10B10A2_UNORM  : 检查 DXGI color space / PQ 元数据
→ B8G8R8A8/R8G8B8A8  : 比较 pre/post 值；若高光已裁切 → 缺陷在更上游
第一个丢失高光信息的节点 = 插入边界候选：____
```

## Hook 点选择（§3.1 优先级）

- 选定：A（捕获完成→颜色转换前）/ B（转换后→编码器前）/ C（编码器输入纹理）/ D（外部桥接兜底）
- 理由与风险：
- 备选：

## Gate P2 结论

- ☐ GO（进入 P3 无损 Hook）　☐ PARTIAL　☐ NO-GO
- 证据文件索引：
