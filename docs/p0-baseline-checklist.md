# P0 基线建立清单（计划书 §4）

> 目标：建立以后每次修改都能重复的"问题基线"。本阶段不对小黑盒进程做任何修改。
> 退出条件（Gate P0）：至少一组"HDR 关闭正常 / HDR 开启异常"的可重复 A/B 样本；客户端版本与系统环境全部记录；远端观看结果稳定复现。

## 1. 环境固定（§4.1）——系统部分已完成（2026-09-05）

运行 `tools/env_fingerprint.ps1` 自动采集，并人工核对以下项：

- [x] Windows 版本与 Build：Windows 11 专业版 25H2，build 26200.9168
- [x] GPU 型号 / 驱动版本 / 驱动日期：NVIDIA GeForce RTX 5070 Ti，驱动 32.0.16.1664（2026-08-24）
      ⚠ 另有 MuMu / GameViewer 虚拟显示适配器（测试目标显示器归属时注意）
- [x] 显示器 HDR 状态、分辨率、刷新率、缩放比例：\\.\DISPLAY1 4K@160Hz，HDR 开启（SDR white 280 nits），缩放 200%
- [x] 小黑盒（黑盒语音 HeyboxChat）PC 客户端版本号：安装版本 1.56.0 / EXE FileVersion 1.31.0，
      目录 `C:\Users\22983\AppData\Local\Qingfeng\HeyboxChat`（指纹见 docs/recon/env-fingerprint-20260905-054128.md）
- [x] 主 EXE 与关键 DLL 的文件版本 / SHA-256（脚本自动算）：同上 JSON
- [ ] 稳定复现 HDR 高光的游戏场景名称与设置：____________（待用户提供）
- [ ] SDR 对照场景：____________（待用户提供）
- [ ] 远端观看端（手机 / 另一台 SDR 显示器）型号与版本：____________（待用户提供）

## 2. 基线样本（§4.2）

每个样本：录屏保存到本地（注明文件名/时间点），并在下表填写远端观察结论。

| 样本 | 条件 | 用途 | 文件/位置 | 远端结论 |
| --- | --- | --- | --- | --- |
| B0 | Windows HDR 关闭，小黑盒直接共享 | 正确 SDR 参照 | 待录 | |
| B1 | Windows HDR 开启，小黑盒直接共享 | 问题样本 | 待录 | |
| B2 | Windows HDR 开启，OBS 正确 HDR→SDR 录制 | 颜色管理正向参照 | 待录 | |
| B3 | HDR 开启，OBS 预览再被小黑盒抓取 | 验证二次桌面合成是否仍影响客户端 | 待录 | |
| B4 | HDR 开启但共享纯 SDR 桌面/浏览器 | 判断问题只针对 HDR 游戏还是整个 HDR 桌面 | 待录 | |

补充：系统级捕获格式探测（`capture_format_spy.exe`）HDR Off / On 对比已完成，
见 `capture-format-ab-analysis.md`。核心结论：**纹理格式不随 HDR 变化**（WGC 跟随请求格式，
DDA 两种状态下都默认 FP16），HDR 判定必须以输出色彩空间为准。

## 3. A/B 复现记录

| 日期 | HDR | 场景 | 远端表现 | 是否可复现 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 2026-09-05 | Off | 系统级 spy（无客户端） | — | — | 格式见 analysis；客户端画面待录 |
| 2026-09-05 | On | 系统级 spy（无客户端） | — | — | 同上 |

## 4. Gate P0 结论

- 结论：☐ PARTIAL（环境指纹与系统级 A/B 完成；B0–B4 远端样本待用户录制后 GO）
- 判定人 / 日期：自动化预采集 2026-09-05
- 下一条最小验证动作：用户启动一次小黑盒屏幕共享（远端有人观看），期间运行
  `module_diff.ps1 -Snapshot -Tag sharing`，与 idle diff 确定活跃 RTC SDK
