# Bootstrap

加载器：仅负责本地加载 hdrfix.dll 与版本检查（目标客户端哈希 → compat.json allowlist）。
**不做**持久化驻留、隐藏进程、绕过安全机制（计划书 §2.1/§12.1）。

- Gate 依赖：P2/P3 之后实现；未知客户端版本默认拒绝写入，只允许只读诊断
- 故障保护要求：Crash marker 检测上次异常退出 → 下次默认旁路启动（§10.3）
