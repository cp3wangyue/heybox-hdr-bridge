# Hook

Hook 边界实现。**Gate P2 确认真实帧路径之前，此目录保持空置。**

- 优先级（§3.1）：A 捕获完成→颜色转换前 > B 转换后→编码器前 > C 编码器输入纹理 > D 外部桥接兜底
- 只 Hook 必要函数；选择许可证清晰的最小 Hook 库（引入前先做许可证评估）
- COM vtable 目标必须记录对象创建/销毁，避免对已释放实例调用（§7.1）
- 每帧路径内禁止：内存分配、文件 I/O、同步 Flush（§8.5/§9.2）
